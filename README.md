Hi, that's my c shell project. In order to run it, you have to be in a Unix env like (Debian...).
Download the shell.c file and compile it using "gcc shell.c -o shell" and run the binary file ./shell
